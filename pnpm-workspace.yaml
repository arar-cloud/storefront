blockExoticSubdeps: true
minimumReleaseAge: 1440 # 24h
trustPolicy: no-downgrade

# Package resolution overrides for stability and security
pnpm:
  overrides:
    # Lock payment-critical versions to prevent transient failures
    'urql': '4.0.6'
    '@saleor/auth-sdk': '1.0.3'
    # Adyen Web SDK: lock to specific minor version to prevent breaking major/minor changes
    # Allow patch updates for security fixes and bug patches
    '@adyen/adyen-web': '^7.0.0 <8.0.0'
    # Security patches
    'graphql': '16.8.1'
    'dset': '>=3.1.4'
    'micromatch': '>=4.0.8'
    '@babel/runtime': '>=7.26.10'
    '@babel/helpers': '>=7.26.10'
    'brace-expansion@>=2': '>=2.0.2'
