version: "3"

services:
  saleor-storefront:
    container_name: saleor-storefront
    build:
      dockerfile: Dockerfile
      args:
        # SECURITY CRITICAL: Build args are read from environment variables only
        # DO NOT hardcode secrets here. Use .env file or orchestrator secrets
        NEXT_PUBLIC_SALEOR_API_URL: ${NEXT_PUBLIC_SALEOR_API_URL}
        NEXT_PUBLIC_STOREFRONT_URL: ${NEXT_PUBLIC_STOREFRONT_URL}
    restart: always
    ports:
      - 3000:3000
    env_file:
      # Load environment variables from external file (never commit secrets to git)
      - .env.local
    environment:
      # SECURITY: Runtime environment variables should only reference non-sensitive configs
      # Use Docker secrets (docker swarm) or mounted secret files for sensitive data
      NODE_ENV: production
    networks:
      - saleor_network

  # Add more containers below (nginx, postgres, etc.)

networks:
  saleor_network:
    external: false
