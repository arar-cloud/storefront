query CurrentUserOrderList($first: Int = 10, $after: String) {
	me {
		...UserDetails
		orders(first: $first, after: $after) {
      pageInfo {
        hasNextPage
        endCursor
        hasPreviousPage
        startCursor
      }
			edges {
				node {
					...OrderDetails
				}
			}
		}
	}
}
